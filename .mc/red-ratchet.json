{
  "schema": "mc-red-ratchet",
  "version": 1,
  "note": "The standing red set on main, seeded from a full npm test run on a clean origin/main worktree at d382274 on 2026-08-22. It may only get smaller: a red name that is not in this list fails the gate. Remove names here when they come good; adding one is raising the floor and is meant to be reviewed as such. See docs/mc-command-matrix.md. 2026-08-29: 15 launchBrokerOwnedSession names (tests/runtime/broker/launch-client.test.js) added \u2014 measured red on a clean origin/main at c52fbfe; they landed through the runner, which merges without this gate.",
  "standing_red": 68,
  "names": [
    "C1 release graph contains every transitive local source edge",
    "Claude accepts a V1 owner id and rejects a forged GitHub socket before readiness",
    "V1 session cutover",
    "V1 session cutover \u203a blocks a nonterminal managed generation as an incompatible runtime claim",
    "V1 session cutover \u203a blocks source-wide duplicate names and ambiguous managed identity",
    "V1 session cutover \u203a refuses exact live legacy sessions without killing or guessing them",
    "a live manifest whose host is gone is unsafe rather than silently reaped",
    "both command dispatchers route destructive maintenance through V1 modules",
    "installed C1 source closure matches its fixed release manifest",
    "launchBrokerOwnedSession",
    "launchBrokerOwnedSession \u203a keeps the verified GitHub boundary when advisory starting presence registration fails",
    "launchBrokerOwnedSession \u203a routes cloud Codex interactive login through device auth without auto-submitting grounding",
    "local auth mode",
    "local auth mode \u203a lifecycle parsers reject removed execution-mode flags",
    "local orchestration has no host gh authority fallback",
    "managed Claude runtime source graph matches its fixed closure",
    "mc cd V1",
    "mc cd V1 \u203a prints the preferred associated directory without assuming a worktree",
    "mc cd V1 \u203a selects one of several workspace associations by opaque id",
    "mc coding-profile argv parsing",
    "mc coding-profile argv parsing \u203a help documents the explicit read-diff-write workflow",
    "mc end/delete V1",
    "mc end/delete V1 \u203a delete is explicit, requires force, and preserves external workspaces",
    "mc end/delete V1 \u203a end archives one exact local session and keeps its external workspace",
    "mc end/delete V1 \u203a end requires an explicit session and never auto-detects a worktree",
    "mc end/delete V1 \u203a legacy destructive end options are rejected before session mutation",
    "mc end/delete V1 \u203a unknown local sessions fail without changing another session",
    "mc gc V1",
    "mc gc V1 \u203a an unsafe live manifest is reported and never reaped",
    "mc new V1",
    "mc new V1 \u203a creates one source-owned session in cwd without Git resources",
    "mc rename V1",
    "mc rename V1 \u203a renames metadata and name claim without moving identity or workspace",
    "mc spawn \u2014 session fabric",
    "mc spawn \u2014 session fabric \u203a legacy project entries migrate to flat V1 sessions without a tree mode",
    "mc spawn \u2014 session fabric \u203a live happy path creates a tracked project session with a brief",
    "mc spawn \u2014 session fabric \u203a missing args \u2192 exit 2 + usage",
    "mc status V1",
    "mc status V1 \u203a reports durable session-home, workspace, and journal state",
    "readPackageCanon",
    "readPackageCanon \u203a reads all three packaged canon files from the real package",
    "requires explicit replacement after a failed generation without a handle",
    "shell-directive emission (\u00a72b)",
    "shell-directive emission (\u00a72b) \u203a mc end keeps the workspace and emits no cd directive",
    "shell-directive emission (\u00a72b) \u203a mc resume emits a cd directive before launch",
    "tests/cli/lifecycle/restart.test.js",
    "tests/cli/lifecycle/resume.test.js",
    "tests/mc/commands/lifecycle.test.js",
    "tests/mc/owned-resource-cleanup.test.js",
    "tests/mc/session-lifecycle-v1.test.js",
    "tests/mc/session-runtime-v1.test.js",
    "tests/runtime/certified-execution/launch-plan.test.js",
    "tests/runtime/session-host/runtime-host.test.js",
    "tests/runtime/session-host/socket-e2e.test.js",
    "tests/runtime/session-host/terminal-screen.test.js",
    "launchBrokerOwnedSession \u203a managed Codex launch carries a token-free GitHub capability without broker credentials",
    "launchBrokerOwnedSession \u203a retries an early Codex SQLite lock and attaches to the successful relaunch",
    "launchBrokerOwnedSession \u203a reconciles an accepted SQLite cleanup after its response is lost",
    "launchBrokerOwnedSession \u203a reconciles an accepted SQLite retry launch before attaching",
    "launchBrokerOwnedSession \u203a bounds repeated Codex SQLite startup retries to two",
    "launchBrokerOwnedSession \u203a does not retry other Codex startup failures",
    "launchBrokerOwnedSession \u203a passes resolved git common dir as a launch option",
    "launchBrokerOwnedSession \u203a does not install a session-scoped gh shim for native launches",
    "launchBrokerOwnedSession \u203a terminalizes a registered starting generation when broker launch fails",
    "launchBrokerOwnedSession \u203a reconciles an accepted live launch after transport timeout without terminalizing it",
    "launchBrokerOwnedSession \u203a waits through transient absence before recovering the exact accepted launch",
    "launchBrokerOwnedSession \u203a fails closed without terminalizing when an ambiguous launch remains absent",
    "launchBrokerOwnedSession \u203a prepares cloud Codex auth and passes scrubbed env to broker launch"
  ]
}
