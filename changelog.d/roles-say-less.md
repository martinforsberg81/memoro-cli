section: Changed

- **The role files say only what their session has to decide.** 431 lines
  across seven roles, 254 after — with `plan.md` untouched, since PR #499 is
  already cutting that one. What went: the proposal template written out twice
  (mc parses nothing now, so the shape is the writer's), roles explaining other
  roles' sessions, the decision lifecycle written in three places, numbered
  meeting choreography, and the *What you never do* lists — mostly naming
  things the session had no route to, or that mc refuses on its own. What
  stayed is judgement a session cannot derive from the code in front of it:
  never a menu, volume is not severity, zero proposals is a good answer, three
  is a lot, the four fields a step may edit and that it is checked.
