section: Fixed

- **Nine small things from `~/mc/proposals/`.** The README no longer
  advertises `mc coding-profile`; `docs/project/README.md` no longer claims to
  be the same text as memoro's; the role catalogue's membership has a test;
  the helper's deploy section reads one commit at two lengths as one commit
  and prints whole shas when the first seven agree; a runner-step fingerprint
  keeps the digits of a project name (`sql-w1-…` and `sql-w10-…` were one
  story); the step role says nothing runs in the background, because
  `mc merge` ends the session under it; a round cut short by a signal writes
  its own end line (`stopped_at: killed`) instead of reading as "round died";
  and the 24-hour line puts every row in one bucket, with `other` for a clean
  helper or intake turn, so its numbers add up to its total.
