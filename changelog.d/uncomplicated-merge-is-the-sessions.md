Every role now carries the rule that an uncomplicated merge is the session's
own to make, with `mc merge` deciding what counts as uncomplicated. A session
running under `mc run` is excluded and says so: the runner is the lander there.
