section: Fixed

- **`mc work release`'s dry run says what the apply does.** On an area
  with no worktree the dry run promised "would remove <conversation>",
  then the apply found the inbox files, kept everything, and said
  "nothing to release" — the printout promised more than the command
  does (measured 2026-08-24). One emptiness forecast now serves both
  modes — what would be left once the removable worktrees are gone, own
  marks aside — and whatever holds the area is named with the way
  forward: "kept — the area and its conversations — it still holds
  inbox (mc work discard <name> removes everything)".
