{
  "schema": "mc-plan",
  "version": 1,
  "goal": [
    "`mc test dev` and `mc test prod` exist: one command puts the running app in front of a session, locally or in production, and measures it with the suites the repository declares. Nothing about them runs by itself.",
    "`mc dev` either exists because something needs it, or does not exist and nothing calls it. It now exists, and `mc test dev` is what needs it \u2014 which is the answer to `mc-dev-1` and the reason this project's scope grew past the question it opened with (ruling 9, 2026-09-05)."
  ],
  "contract": [
    "The cut is not presumed wrong. `mc-cut` removed thirteen verbs on a measurement that was correct inside `memoro-cli`, and step 1 measured the inventory again and recommended remove on numbers nobody disputes. What changed is that a reader now exists. The verb comes back at three: `list`, `register`, `unregister`, the three `scripts/lib/mc-dev-service.mjs` calls \u2014 not a revival of the thirteen-verb session manager.",
    "No caller is left probing a verb that does not exist. A path that fails softly and logs a warning nobody tails is the state this project ends.",
    "`invokeMcDev`'s contract is matched rather than replaced: `mc dev list --json` as the probe, `mc dev <verb> <manifestPath> --json` as the call. memoro's caller is not changed to suit mc.",
    "The environment verbs run only when somebody asks. No runner step, no gate round, no nightly and no session launch calls them (Martin, 2026-09-05).",
    "mc holds no list of memoro's script names. The repository declares what it may be measured with, and mc reads the declaration."
  ],
  "out_of_scope": [
    "The other twelve verbs `mc-cut` removed. Nothing is known to call any of them and this project does not go looking.",
    "Rebuilding memoro's suites. The base-URL seam under them is in scope; what they assert is not.",
    "Why memoro's dev server exits under a long browser round. Measured twice here and filed as a proposal (`2026-09-05-dev-server-exits-under-browser-round`), not chased.",
    "Teaching `reach.mjs` to see cross-repository callers. The lesson is written into `docs/technical/mc-cut.md`; the tool is not taught to read `mc.log`."
  ],
  "success_criteria": [
    {
      "met": true,
      "criterion": "Nothing on this machine calls a verb `mc` does not have.",
      "check": "Ran `npm run dev` in a memoro worktree with this mc on PATH: the wrapper logged `mc dev: registered dev-715b6e40-\u2026 for memoro`, `mc dev list` showed it, `npm run dev:stop` unregistered it. `~/.memoro/mc/logs/mc.log` now records `verb: dev` ending `exit_code: 0` where all 1 132 before it ended 2."
    },
    {
      "met": true,
      "criterion": "What the cross-worktree inventory was for is written down, and works.",
      "check": "`docs/dev-server-protocol.md` \u00a7 *Who reads the inventory*, and ruling 9 in `docs/project/mc/rulings.md`."
    },
    {
      "met": true,
      "criterion": "One command measures the running app, in dev and in prod.",
      "check": "`mc test dev --here` brought a server up, reused it on the second call, and ran the declared suites against it; `mc test prod --here --suite msr-async-state` ran green against meetmemoro.app. The write smoke signed in, wrote, read back, deleted and verified the deletion against a dev server."
    },
    {
      "met": true,
      "criterion": "A suite that did not run is never reported as green.",
      "check": "`.mc/test.json` declares `skip_exit_code: 78`; `mc test prod --suite write-smoke` with no token prints `\u00b7\u00b7` and `did not run`, and counts 0 green."
    }
  ],
  "documents": [
    {
      "label": "The protocol, rewritten to the surface that exists",
      "path": "../../../dev-server-protocol.md"
    },
    {
      "label": "What `mc-cut` measured, and the edge it could not see",
      "path": "../../../technical/mc-cut.md"
    },
    {
      "label": "The ruling this project ends on",
      "path": "../rulings.md"
    }
  ],
  "steps": [
    {
      "title": "Measure what was actually lost, then put the choice",
      "status": "done",
      "done_when": "The PR says what the dev-service registration did that nothing else does, names which repository does the remaining work under each option, and carries one recommendation for Martin.",
      "instruction": [
        "Start by narrowing the claim, because the proposal that raised this overstated it and the overstatement points the decision the wrong way. It says every workarea's *\"mechanism that detects a colliding second instance\"* has been a no-op since the cut. Read `scripts/dev.mjs` in memoro before accepting that: the same-worktree collision check is a **local lock file** \u2014 `LOCK_FILE` at `dev.mjs:51` is `<REPO_ROOT>/.wrangler/dev-server/run/dev.lock.json`, claimed with `fsp.open(\u2026, \"wx\")` at `:361`, with a stale-pid sweep and a `verifySameWorktreeWrapper` check at `:373`\u2013`:406`. It never touches mc and it still works. What went with the verb is the **cross-worktree inventory**: mc's own record of which dev servers are running in which workareas.",
        "So the real question is what that inventory was for, and the honest answer may be \"nothing that is still true\". Find out rather than assume: `git log -S\"mc dev\"` in memoro and `git log` around the `minor-fixes` project (closed 2026-08-29, which built the probe-then-register path deliberately) will say what problem it was answering. If it was answering \"two workareas both bound port 8787\", check whether `PORT_FALLBACK_BASE`/`PORT_FALLBACK_SPAN` (`dev.mjs:41`) already handle that without any inventory at all.",
        "Measure the cost of the dead path as it stands. It is not free: `invokeMcDev` (`scripts/lib/mc-dev-service.mjs:115`) spawns `mc dev list --json` before every register and unregister, with `MC_PROBE_TIMEOUT_MS`, and every one of those is a process spawn that exits 2. `~/.memoro/mc/logs/mc.log` holds **1 132** `\"verb\":\"dev\"` entries, every one of them `sub: list`, `--json`, `exit_code: 2` \u2014 and the count is still growing: the most recent when this plan was amended was 2026-09-05T13:47:09Z. They are not all workareas; at least one is `~/memoro` itself, so a person running `npm run dev` by hand pays it too.",
        "Then put the two options with what each costs. **Restore** \u2014 `list`, `register`, `unregister` next to `mc work` and `mc repo`, matching `invokeMcDev`'s existing contract exactly (`mc dev list --json` as the probe, `mc dev <verb> <manifestPath> --json` as the call). Small and self-contained, and it means mc carries an inventory again, which is a thing to maintain. **Remove** \u2014 pull the calls out of `scripts/dev.mjs` and `scripts/lib/mc-dev-service.mjs`, and rewrite `tests/scripts/mc-dev-service.test.js:176`, which currently pins the removed contract in its own name: *\"capability must be decided by running mc dev list, not by grepping --help text\"*. Cheaper, and it makes the loss of the inventory explicit instead of silent.",
        "Say plainly which repository does the work under each, because it decides where the next plan lives and this repository's runner cannot do memoro's half. A project's plan determines its repository \u2014 `repoOf` resolves the plan's location to one repository and the workarea is `~/mc/<name>/<repo>` \u2014 so a step here can only change `memoro-cli`. Under *restore*, step 2 of this plan is the whole of it and memoro changes nothing. Under *remove*, the work is entirely memoro's and needs a plan under memoro's `docs/project/`, which is Martin's planning session to open, not this step's to create.",
        "One recommendation, in the PR body. On the evidence so far \u2014 the same-worktree guard is intact and local, and nothing in `memoro-cli` has wanted the inventory since the cut \u2014 *remove* is the shorter road and the one the cut already chose; say so if the measurement agrees, and say so if it does not.",
        "Carry Martin's answer into `docs/project/mc/rulings.md` in the shape that file uses. Nothing else in mc records a decision."
      ],
      "comments": [
        "Measured, and the measurement is in the PR body. The plan's framing holds in both directions: the same-worktree collision guard is `dev.lock.json` plus `verifySameWorktreeWrapper` and never touches mc, and port de-confliction is `deterministicPortCandidates` plus a real `net.createServer` bind probe \u2014 neither has ever consulted the inventory. What the registration did that nothing else does is one machine-wide index of running dev servers keyed by `session_name`/`coding_session_id`, and a verified stop/restart argv for a server you did not start.",
        "The usage numbers are the reason for the recommendation, and they are from `~/.memoro/mc/logs/mc.log`, which starts 2026-08-05. In that month `mc dev` was invoked 565 times: 370 `list --json`, 93 `register --json`, 92 `unregister --json` \u2014 all of those are `invokeMcDev` talking to itself \u2014 against 2 human `mc dev list`, 1 `status`, 1 `plan`, 6 `ensure`, and zero `stop`, `restart` or `logs`. `~/.memoro/mc/dev-servers/` holds 33 manifests, oldest 2026-07-26, and not one has a live pid: nothing reaped them even while the verb existed.",
        "The dead-probe rate is higher than the proposal said: 131 exit-2 `dev` invocations on 2026-09-04 and 43 by 16:40 on 2026-09-05, not 22/day. Each costs 8-42 ms, so the cost is the misleading `mc dev inventory unavailable` warning in every dev-server log, not CPU.",
        "Recommendation is *remove*, and the work is memoro's: `scripts/dev.mjs` (imports :29-35, `publishMcDevService` :418, `retireMcDevService` :465, call sites :139/:166/:308/:322/:1075), `scripts/lib/mc-dev-service.mjs`, `tests/scripts/mc-dev-service.test.js`, `tests/scripts/dev-script.test.js`, the `mc-dev-service` entry in `config/testing-registry.mjs`, `docs/technical/dev-service-registration.md`, `README.md:297-298`, and `.mc/dev.json` (whose `runtime-manifest` readiness only ever had `mc dev ensure` as a reader). That needs a plan under memoro's `docs/project/`; this repository's runner cannot reach it, which is also why success criteria 1 and 3 cannot be met from this plan under *remove*.",
        "Two findings for step 2 that make *remove* not quite a no-op in `memoro-cli`, neither of them blocking: `devServersRoot()` at `src/mc/paths.js:33` survives the cut with no caller anywhere in `src/` or `tests/`, and `docs/dev-server-protocol.md` (226 lines) still documents the whole removed surface, `mc deps` and `mc setup --dependency-mode` included. Under *remove* those are what this repository's half actually is, plus sweeping the 33 orphaned files in `~/.memoro/mc/dev-servers/`.",
        "Blocked on Martin's answer rather than done, deliberately: step 2 branches on a ruling that does not exist yet, and a `ready` step nothing can satisfy is the livelock the 2026-09-04 ruling in `docs/project/mc/rulings.md` was written about. Whoever carries the answer writes it into `rulings.md` in that file's shape, cites it as `mc-dev-1`, and sets this step `done` and step 2 `ready`. The question is written up at the workarea root as `mc-dev-1`.",
        "Answered by Martin on 2026-09-05, in the planning session rather than in a brief: restore, because `mc test dev` is the reader the measurement said the inventory did not have. Written up as ruling 9. The recommendation in #628 was *remove* and it is not overturned \u2014 its own closing sentence is what answers it."
      ],
      "pr": 628,
      "blocked_by": null
    },
    {
      "title": "Build the inventory, and the verbs that read it",
      "status": "done",
      "done_when": "`mc dev list|register|unregister` answers `invokeMcDev`, and `mc test dev|prod` measures a running app with the suites the repository declares.",
      "instruction": [
        "Delivered by hand in Martin's own session on 2026-09-05 rather than by a step round, because the shape of `mc test dev|prod` was being decided with him as it was built.",
        "memoro: one base URL for all twenty browser suites (#11452), `.mc/test.json` declaring what mc may measure the repository with (#11456), and the write smoke (#11469).",
        "memoro-cli: `mc dev` and `mc test dev|prod` (#635)."
      ],
      "comments": [],
      "pr": 635,
      "blocked_by": null
    },
    {
      "title": "Close out",
      "status": "done",
      "done_when": "`docs/technical/mc-cut.md` records what the cut cost and how it was found, the cross-worktree inventory's fate is written down, and `docs/project/project_log.md` has the row.",
      "instruction": [
        "The lasting finding is in `docs/technical/mc-cut.md` \u00a7 *The edge it cost a verb*: `reach.mjs` is a same-repo static import graph, and a sibling repository's `execFile('mc', \u2026)` is invisible to it by construction. `mc.log` would have shown 555 calls from ten worktrees.",
        "A second gap in the same tool was found while fixing the first and is written up beside it: the hand-kept `LIVE` seed list had already drifted, and `mc deploy` \u2014 routed, working, 379 lines \u2014 was being reported dead.",
        "`docs/dev-server-protocol.md` is rewritten to the surface that exists, and says what it used to describe."
      ],
      "comments": [],
      "pr": null,
      "blocked_by": null
    }
  ]
}
