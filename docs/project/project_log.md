# Project log

One row per project closed out of `docs/project/`. Append-only — a row is
never edited to reflect later events; a project reopened after being logged
gets a new row when it closes again.

Same file, same fields and same rule as memoro's `docs/project/project_log.md`;
`mc helper` reads whichever of the two the repository it is looking at keeps.

## Why these fields

The test for each field is: what would someone need, reading this in two
years, having forgotten everything about the project except that it once
existed?

- **date** — when it was archived. Without this the log cannot be read in
  order, and "recent" or "old" becomes a guess.
- **programme** — which initiative it belonged to, so entries can be grouped
  without re-deriving the mapping from memory or from `docs/project/`, which
  by then no longer has the directory.
- **project** — the mc workarea name. This is the join key back to git
  history: `git log --all -- docs/project/<programme>/<project>` (or its
  pre-archive path) is how a real question about *why* gets answered, once
  the one-line summary here isn't enough.
- **outcome** — `delivered`, `abandoned`, or `superseded`. A closed project
  is not always a finished one, and "what happened to it" is usually the
  first question, before "what did it do".
- **summary** — one line: what it built, or why it stopped. Enough to decide
  whether to dig further, not a report.
- **doc** — the path under `docs/technical/` that now describes the resulting
  architecture, per the close-out rule in `README.md`. `none` when the project
  produced no lasting architecture (abandoned before shipping, or superseded
  before merging).
- **pointer** — a PR number, commit SHA, or short git-log-worthy reference
  into the history that this row summarizes. The row is a one-line index,
  not the record; this is how a reader gets from the index to the record.

## Log

| date | programme | project | outcome | summary | doc | pointer |
|---|---|---|---|---|---|---|
| 2026-08-29 | mc | mc-ui | delivered | Made bare `mc` the one page — NOW, QUEUE, DECISIONS, INTAKE, WORK with the menu under it — in 0.1 s offline instead of 1.9 s, gave the runner and the foreground verbs the two files that say what is running this second, and removed every other surface that printed a list. | [docs/technical/mc-ui.md](../technical/mc-ui.md) | [#430](https://github.com/martinforsberg81/memoro-cli/pull/430), [#435](https://github.com/martinforsberg81/memoro-cli/pull/435), [#440](https://github.com/martinforsberg81/memoro-cli/pull/440), [#441](https://github.com/martinforsberg81/memoro-cli/pull/441), [#443](https://github.com/martinforsberg81/memoro-cli/pull/443) |
| 2026-08-29 | mc | mc-ui-polish | delivered | Gave the page a palette: a step kind and a plan status one colour each wherever they are printed, the header, the five sections and the clock painted for meaning, every escape added after the width was decided — no new section, datum or flag, and the plain page unchanged but for one always-drawn `●` on a DECISIONS row. | [docs/technical/mc-ui.md](../technical/mc-ui.md) | [#446](https://github.com/martinforsberg81/memoro-cli/pull/446) |
| 2026-08-29 | mc | mc-helper | delivered | Gave production a reader: `mc helper --collect` writes a daily digest of five memoro sources into `~/mc/intake/errors-<date>.md` with the delta against the previous one — the digest is its own state, because memoro records no baseline anywhere — and `mc helper` then runs one headless Sonnet turn that writes proposals nobody acts on but Martin. `mc run` runs it once a day, gated by its own runs.tsv row; `mc brief --collect` lists the proposals and the page shows the loud lines. Retiring memoro's `sync-todo.mjs` (decision mc-2, on or after 2026-09-05) is handed to a memoro-side plan. | [docs/technical/mc-helper.md](../technical/mc-helper.md) | [#424](https://github.com/martinforsberg81/memoro-cli/pull/424), [#439](https://github.com/martinforsberg81/memoro-cli/pull/439), [#445](https://github.com/martinforsberg81/memoro-cli/pull/445) |
| 2026-08-29 | mc | mc-run-lanes | delivered | Made `mc run` drive one lane per repository at the same time inside the one process: the round splits the queue by the repo each plan lives in, `~/mc/runner/current.json` became one `current-<repo>.json` per lane and NOW a list, the Claude quota is one sleep both lanes wait out and STOP ends both after the step each is in, and the session became a promise because a synchronous wait held the event loop for the whole budget. No new command or flag, and one repository with ready plans is still one lane. | [docs/technical/mc-run.md](../technical/mc-run.md) | [#450](https://github.com/martinforsberg81/memoro-cli/pull/450) |
| 2026-08-29 | mc | mc-tidy | delivered | Made `status: done` the whole trigger: `mc run` archives the plan (directory `git rm`'d, `project_log.md` row written unless the close-out step wrote one, one PR per repository) in the round it reads it, and at the end of that round closes every workarea whose plan is done, whose worktree is clean and whose last run merged — worktree handed back, local branch deleted, whatever the folder kept moved to `runner/log/closed/`. `queue.md` became a strict list of names that empties itself, the page and the runner agree that a workarea is a folder holding `memoro/` or `memoro-cli/`, and the two things a machine must not decide — a project archived with no `docs/technical/` note, a workarea with no plan on main — are written to `~/mc/intake/` and raised in `mc brief`. No new command, flag or prompt key. | [docs/technical/mc-tidy.md](../technical/mc-tidy.md) | [#451](https://github.com/martinforsberg81/memoro-cli/pull/451), [#454](https://github.com/martinforsberg81/memoro-cli/pull/454), [#455](https://github.com/martinforsberg81/memoro-cli/pull/455), [#456](https://github.com/martinforsberg81/memoro-cli/pull/456), [#457](https://github.com/martinforsberg81/memoro-cli/pull/457) |
| 2026-08-29 | mc | mc-brief | delivered | Made the hour Martin sits down a verb: `mc brief --collect` gathers nine sections — merged, open, every decision file with no `**Beslut:**` line, the helper's proposals, every plan on both origin/mains, what the tidying left, the runner's day, the queue — into `~/mc/brief/<date>.md` with no model in 1.5 s online and 0.2 s offline, and `mc brief` then opens a fresh foreground Opus session on that file with the `brief` role: one decision at a time, each a proposal Martin says GO to, its only write a `**Beslut:**` line. No daemon, no watcher, no inbox, and the runner neither knows nor needs it. The answer travels plan-first — the next step session writes it into PLAN.md and sets `ready`, and the collect deletes the file once no plan waits on it. | [docs/technical/mc-brief.md](../technical/mc-brief.md) | [#413](https://github.com/martinforsberg81/memoro-cli/pull/413), [#416](https://github.com/martinforsberg81/memoro-cli/pull/416), [#422](https://github.com/martinforsberg81/memoro-cli/pull/422), [#433](https://github.com/martinforsberg81/memoro-cli/pull/433) |
| 2026-08-29 | mc | mc-dormant | delivered | Executed decision mc-1: `mc pm` and `mc pm-helper` answer one line and exit 2 with their machinery kept for the later cut, and the whole `mc watch` programme — the PM round, the sessions watchman, the notices ledger and the wake queue, 29 files and 5 864 lines — is deleted together with `~/.memoro/mc/watch/`, so a refused wake is reported rather than promised to a watcher that no longer exists. `mc worker` reads `canon/roles/worker.md` instead of a catalogue mc does not install, and its overlay escalates by writing `../decisions/<project>-<date>.md` rather than messaging a PM. `mc --help` now describes one world: plans under `docs/project/`, the runner that takes their steps, `mc brief` for decisions, `mc plan` and `mc worker` for the sessions Martin drives himself. | [docs/technical/mc-dormant.md](../technical/mc-dormant.md) | [#419](https://github.com/martinforsberg81/memoro-cli/pull/419), [#423](https://github.com/martinforsberg81/memoro-cli/pull/423), [#459](https://github.com/martinforsberg81/memoro-cli/pull/459) |
| 2026-08-29 | mc | mc-merge | delivered | Made `mc merge <repo> <pr>` the one door a pull request lands through: the gate round unchanged under a new name (`mc repo merge` prints where it went and exits 2, not aliased), and `--docs` landing a pull request whose every file is under `docs/` with no suite, no lease, no worktree and no model — the file list read from GitHub rather than a local diff, a draft, a closed one and the first path outside `docs/` each refused by name, and the round written to the round log as `mode: docs`. A plan PR now lands seconds after it is opened, by the session that opened it, so the runner sees it: six such rounds on 2026-08-28/29 (memoro-cli #419, #420, #428, #429; memoro #11025, #11039), all merged, none clicked, one of them the runner's own triage. The last word a session hears was the third place that had to say so — `mc plan`'s own first prompt, not only the role and the runner's triage prompt. | [docs/technical/mc-merge.md](../technical/mc-merge.md) | [#418](https://github.com/martinforsberg81/memoro-cli/pull/418), [#425](https://github.com/martinforsberg81/memoro-cli/pull/425), [#460](https://github.com/martinforsberg81/memoro-cli/pull/460) |
| 2026-08-29 | mc | mc-plan | delivered | Made `mc plan <name>` how new work enters the system: a fresh foreground Opus session in the workarea (created from `origin/main` if missing, never tmux, never `--resume`, launched through `work-open.js` like every other verb) carrying `canon/roles/plan.md` — read from the package, never from the user's catalogue — whose deliverable is `docs/project/<programme>/<name>/PLAN.md` in a PR titled "Plan: <name>" that the session lands itself with `mc merge <repo> <pr> --docs`, because the runner's queue is built from main. The overlay reaches both tools as one body with the Coding Profile: `--append-system-prompt` for claude, `-c instructions=` for codex — a channel that already existed, so `instructionsFor` stopped dropping the overlay for codex and the planned `AGENTS.md` fallback was never needed, and nothing is written into the worktree. The role fixes the programme rule (extend, never fork), the PLAN.md shape a headless step session reads, and decisions as `../decisions/<programme>-<n>.md` proposals with one recommendation. | [docs/technical/mc-plan.md](../technical/mc-plan.md) | [#412](https://github.com/martinforsberg81/memoro-cli/pull/412), [#414](https://github.com/martinforsberg81/memoro-cli/pull/414), [#425](https://github.com/martinforsberg81/memoro-cli/pull/425), [#426](https://github.com/martinforsberg81/memoro-cli/pull/426) |
| 2026-08-30 | mc | mc-run | delivered | Moved the runner into mc: `mc run [--rounds N] [--once] [--no-merge] [--idle-sleep S]`, rounds 0 = forever, replaced 220 lines of bash calling `claude -p`, so the tool is a per-project choice through the launch adapter (`tool: codex` runs a step as `codex exec --json --sandbox danger-full-access`, its usage read from the event stream), the prompts are `canon/roles/{step,reconcile}.md` over the Coding Profile, and runs.tsv is something `mc status` reads. A round is the queue from `~/mc/queue.md` plus every ready plan on both origin/mains, one lane per repository at a time; a step is a fresh headless session in a workarea created on demand, main merged in and never rebased, and the PR squash-merged with one wait-and-retry. The runner decides nothing with a model and nothing about plans: no triage, no decision files, `waiting-decision` is simply not ready. Measured from 2026-08-28T23:28Z: 115 rows, 84 merged, none left open, one quota sleep, no rebase failure. | [docs/technical/mc-run.md](../technical/mc-run.md) | [#421](https://github.com/martinforsberg81/memoro-cli/pull/421), [#462](https://github.com/martinforsberg81/memoro-cli/pull/462), [#465](https://github.com/martinforsberg81/memoro-cli/pull/465), [#475](https://github.com/martinforsberg81/memoro-cli/pull/475) |
| 2026-08-30 | mc | mc-status | delivered | Made `mc status <name>` the one-project answer — its PLAN.md read from the workarea rather than one-step-behind main and told when the two differ, the decisions that are actually its own (its area, its name, or the programme's numbered file), its last three rows of `runs.tsv` and the open PR on its branch — in 1.7–2.2 s live and 0.13 s `--offline`, no model, nothing written. The four blocks it was planned as became `mc` instead (decision mc-3), so `status-collect.js` and the dated list-price table in `prices.js` are what the page borrows, and the bare verb says where the page went. The old session/lease/watcher board had already gone whole with mc-ui; what was left to retire was the sentence pointing at `mc --watch`, a page removed the day it landed, and its row in `docs/mc-command-matrix.md` — the test now runs every `mc …` that sentence offers. | [docs/technical/mc-status.md](../technical/mc-status.md) | [#415](https://github.com/martinforsberg81/memoro-cli/pull/415), [#427](https://github.com/martinforsberg81/memoro-cli/pull/427), [#476](https://github.com/martinforsberg81/memoro-cli/pull/476) |
| 2026-08-31 | mc | mc-test-round | delivered | Stop this repository's own selector matching a directory as a pin | [docs/technical/mc-merge.md](../technical/mc-merge.md) | [#501](https://github.com/martinforsberg81/memoro-cli/pull/501), [#502](https://github.com/martinforsberg81/memoro-cli/pull/502), [#503](https://github.com/martinforsberg81/memoro-cli/pull/503), [#504](https://github.com/martinforsberg81/memoro-cli/pull/504), [#505](https://github.com/martinforsberg81/memoro-cli/pull/505) |
| 2026-09-02 | mc | mc-run-one-project | delivered | Made a round ask the plan before it asks git: `planRefusal` puts `kindFor` — the page's own reading of the `PLAN.json` files `queue()` has already fetched from both `origin/main`s — in front of `runStep`, so a project whose plan is blocked, done or unparseable is passed over without a worktree, a `git status`, a fetch or a merge, and the same filter is re-applied when a lane stays on a project after a merged step. Those refusals leave one line for the whole round, counted by reason in the page's shape and naming the plans that do not parse, while everything the plan cannot carry — a dirty worktree, a pull request in flight, a merge that conflicted, a branch that cannot be pushed — keeps its own named line. Measured 2026-09-02: `mc run --rounds 1` against `~/mc` walked 38 projects in 51 s and started none of them; 36 of those 38 answers were already on the page, and answering them from the plans in hand is 1.2 ms. No new file, cursor or queue, and the order stays `queue()`'s. | [docs/technical/mc-run.md](../technical/mc-run.md) | [#529](https://github.com/martinforsberg81/memoro-cli/pull/529), [#532](https://github.com/martinforsberg81/memoro-cli/pull/532), [#PR](https://github.com/martinforsberg81/memoro-cli/pull/PR) |
| 2026-09-02 | mc | runner-open-prs | delivered | Made the runner ask GitHub what is open before it acts, and land through one door. A round now reads three things instead of two — `origin/main` for the queue, the worktree for the plan, and `gh pr list --state open` once per repository — and an open pull request on a project ends that project's round with a line naming it, which also covers the timed-out session and the `plan-trespass` step that each leave a branch carrying unlanded work. A workarea whose branch is already in `origin/main` by content is moved to `<name>-<n>` *before* a session starts, where the push-guard used to refuse the push after it; 44 of the workareas under `~/mc` stood on such a branch on 2026-09-02. `mergePr` and its `gh pr merge --squash` are gone: a finished step lands through `repo-merge.js` in process, and the row reads `merged` only when `merged_into` was `main` — the old merge had squashed #11250 into the branch of #11249 and logged `success,merged` while `main` received nothing. A stack is ordered by `stackOrder` or none of it lands. And the top-level `what_the_code_taught_us` became `steps[i].comments`, plain paragraphs on the step: three of the five trespasses that day were malformed entries in that shared field, and `new-user`'s plan sat unreadable on `origin/main` for a day for the same reason — a plan the schema refuses now gets a row in `~/mc/intake/unreadable-plans.md` where `mc brief` raises it, instead of a `runner.log` line nobody reads. 259 entries moved off 38 plans. | [docs/technical/mc-run.md](../technical/mc-run.md) | [#519](https://github.com/martinforsberg81/memoro-cli/pull/519), [#522](https://github.com/martinforsberg81/memoro-cli/pull/522) |
| 2026-09-03 | mc | mc-live-page | delivered | Made `mc` left open at a terminal keep telling the truth: every 30 s the rows that changed are rewritten where they stand and nothing else on the screen is written to — not the scrollback above, not the key lines, not the prompt, not the cursor in a half-typed answer. `page-frame.js` is the pure differ (relative `CSI` moves only, four cases asserted as bytes with no terminal: unchanged writes nothing at all, a changed row is one move and one `CSI 2K`, a shrinking frame keeps its footprint because `CSI M` would move rows the page does not own, and a grown frame is reprinted with `CSI 0J` because `CSI L` drops the bottom of the screen instead of scrolling into the scrollback); `page-live.js` is the loop, with the interval a floor measured from the end of the last collect so a slow read delays the next frame rather than queueing one behind it, a failed collect holding the last good frame with one line saying so, and a raw-mode reader on its own `/dev/tty` descriptor — raw because a growth frame reprints everything below the page and canonical mode hides the typed characters in the kernel. No alternate screen buffer, no configuration, no new flag; a terminal narrower than 60 columns gets the old non-live reader, rows scrolled off the top are never rewritten, and a pipe is unchanged and now pinned as bytes. Checked in a real pty on 2026-09-03 (`scripts/mc-live-page-check.mjs`, 5½ minutes, not in `npm test`): ten refreshes, the marker above the page still there once, the page on screen once, the typed `1` and its cursor intact, `stty -g` identical before and after. The refresh costs one `collectPage` — 1.1–1.8 s warm on 2026-09-03 against the 0.1 s recorded on 2026-08-29, because `plans.json` is keyed by the `origin/main` sha and the runner moves it. | [docs/technical/mc-ui.md](../technical/mc-ui.md) | [#544](https://github.com/martinforsberg81/memoro-cli/pull/544), [#546](https://github.com/martinforsberg81/memoro-cli/pull/546), [#547](https://github.com/martinforsberg81/memoro-cli/pull/547) |
| 2026-09-03 | mc | nightly-full | delivered | Red, and since when | none | [#549](https://github.com/martinforsberg81/memoro-cli/pull/549), [#550](https://github.com/martinforsberg81/memoro-cli/pull/550), [#551](https://github.com/martinforsberg81/memoro-cli/pull/551) |
| 2026-09-03 | mc | runner-truth | delivered | A runner that merges its own code hands over to it | [docs/technical/mc-run.md](../technical/mc-run.md) | [#553](https://github.com/martinforsberg81/memoro-cli/pull/553), [#554](https://github.com/martinforsberg81/memoro-cli/pull/554), [#555](https://github.com/martinforsberg81/memoro-cli/pull/555), [#556](https://github.com/martinforsberg81/memoro-cli/pull/556) |
| 2026-09-03 | mc | mc-cut | delivered | Took the session manager out from under the page: `src/` went from 281 files and 80 345 lines to 153 and 40 945, `tests/` from 263 files to 155, `docs/plans/` from 34 files to 2, and `reach.mjs` — the static import graph seeded from the page and its verbs — went from 71 % of `src/` unreached to 0 %. The order was the whole method: the verb left a router first, and only then was what nothing else reached deleted, so every cut is one revert. The routers are `src/mc-cli.js` (the page plus twelve verbs) and `src/bin-mc.js` (`mc vault`, and nothing else); the workarea `handoff` concept and the `inbox` channel went with their readers and writers. Three things survive that no verb reaches, each named rather than left to be rediscovered: vault's carcass — 9 451 lines the two `credential-domain` files import at module top level, kept so those two can load, and already unrunnable because its own pinned path lists name files this project deleted; `memoro` / `memoro-cli`, 20 files that `package.json`'s other two `bin` entries reach and that no step earned the right to delete; and four files spawned by a path literal that no import graph can see. `reach.mjs` is kept as `scripts/reach.mjs` and `npm run reach`, out of `npm test` on purpose. Still untrue and named in the note: `README.md`, `docs/onboarding.md` and `scripts/mc-release-smoke.js` all still describe the deleted product. | [docs/technical/mc-cut.md](../technical/mc-cut.md) | [#538](https://github.com/martinforsberg81/memoro-cli/pull/538), [#539](https://github.com/martinforsberg81/memoro-cli/pull/539), [#540](https://github.com/martinforsberg81/memoro-cli/pull/540), [#543](https://github.com/martinforsberg81/memoro-cli/pull/543), [#561](https://github.com/martinforsberg81/memoro-cli/pull/561), [#564](https://github.com/martinforsberg81/memoro-cli/pull/564) |
| 2026-09-04 | mc | step-parallelism | delivered | The lane count is a setting, and `mc step` is how it is set | none | 65c8e2f |
| 2026-09-04 | mc | mc-workarea-deps | delivered | Gave every place mc runs this repository's tests one dependency tree instead of a copy per place: `~/mc/node_modules`, installed once in the work root from a copy of the repository's `package.json` — minus `scripts` and `bin`, whose `postinstall` names a path that does not exist there — and `package-lock.json`, kept in step by a byte comparison of the lockfile, and above every workarea rather than inside one, because node walks `node_modules` up every parent while a `node_modules` in the checkout is visible to git and to `scripts/affected-tests.js`, where a symlink is not matched by `.gitignore` and cost 250 selected files instead of 41. The merge gate's throwaway worktree moved out of `~/.memoro/mc` to `<work root>/gate/<repo>/candidate` so it stands in that same tree and installs nothing, and `dependencyTree` replaced the existence check with node's own parent walk: a round now stops when a declared package resolves from nowhere, where `prepare: null` and a sentence in `repo-gate-table.js` used to vouch that the suite ran without a tree — false for months while the five `src/runtime/session-host/` test files went unrun and uncounted, once per round, with the reassuring line printed. Those five were deleted by mc-cut (#561) one day after they were measured, so the project's criterion was read at the level that survived them (decision workarea-deps-1): a round with no `npm ci`, no vouch line and every declared package resolving — 73 files, 952 tests, 0 red selected on #570 and 143 files, 1538 tests, 0 red full on 2026-09-03, and `mc test memoro-cli --full` at 44198c6 on 2026-09-04 green with no prepare step. | [docs/technical/mc-work.md](../technical/mc-work.md) | [#537](https://github.com/martinforsberg81/memoro-cli/pull/537), [#541](https://github.com/martinforsberg81/memoro-cli/pull/541), [#570](https://github.com/martinforsberg81/memoro-cli/pull/570), [#578](https://github.com/martinforsberg81/memoro-cli/pull/578) |
| 2026-09-04 | mc | merge-repair | delivered | Close-out | [docs/technical/mc-run.md](../technical/mc-run.md) | [#584](https://github.com/martinforsberg81/memoro-cli/pull/584), [#585](https://github.com/martinforsberg81/memoro-cli/pull/585), [#586](https://github.com/martinforsberg81/memoro-cli/pull/586), [#587](https://github.com/martinforsberg81/memoro-cli/pull/587) |
| 2026-09-04 | mc | mc-deploy | delivered | Gave production one door: `mc deploy [--dry-run]` prints what would ship — the sha of memoro's `origin/main`, what `deploys.tsv` or `/api/version` says is live, the gap in commits, and whether the nightly ever measured that tree whole — asks once at a terminal, and on a yes runs the repository's own `npm run deploy` under memoro's repository lease, so no gate round or landing moves `main` while the build reads it. No flag skips the question, without a TTY it exits 2, and the runner never calls it: deploying is Martin's word every time. `scripts/deploy.mjs` is still the whole deploy — mc reimplements no step of it, invents no flag for it and returns its exit code — and what mc adds is the record: a row in `~/mc/runner/log/deploys.tsv` written *before* the spawn saying `running` and completed after it, so a deploy that dies half-way is a row that says where it stopped rather than a gap somebody reconstructs from `/admin/deploy/logs`, and a `no`, a held lease or no terminal is a `refused` row because it is a deploy somebody meant to make. `stopped_at`, `build` and the verified `live_commit` are parsed out of four named lines of the script, tolerantly, by teeing its output rather than inheriting it. Three readers then agree because they read the same row: the page's RUNNER block draws `production <sha> · deployed <age> ago by <holder>` and yellow when `/api/version` names another sha or a `running` row is an hour old, the brief gained a *Production* section and the role a sentence saying a `main` well ahead of production with a green nightly is a deploy to propose in one line, and `helper-collect.js`'s `deployState` takes the age from the freshest of the row and the webhook log. `/api/version` became the helper's sixth source, cached to `~/mc/runner/version.json`, because the page must fetch nothing. Measured: `mc deploy --dry-run` against `~/memoro` on 2026-09-04 read `e30fd83`, live `b3e65b6` (build 23533), 21 commits to ship, the nightly 79 commits behind. Not measured: everything from the question onwards has only ever run against a faked script — the one real deploy is Martin's to type. | [docs/technical/mc-deploy.md](../technical/mc-deploy.md) | [#592](https://github.com/martinforsberg81/memoro-cli/pull/592), [#594](https://github.com/martinforsberg81/memoro-cli/pull/594), [#595](https://github.com/martinforsberg81/memoro-cli/pull/595) |
