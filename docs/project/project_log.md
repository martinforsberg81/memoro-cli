# Project log

One row per project closed out of `docs/project/`. Append-only — a row is
never edited to reflect later events; a project reopened after being logged
gets a new row when it closes again.

Same file, same fields and same rule as memoro's `docs/project/project_log.md`;
`mc helper` reads whichever of the two the repository it is looking at keeps.

## Why these fields

The test for each field is: what would someone need, reading this in two
years, having forgotten everything about the project except that it once
existed?

- **date** — when it was archived. Without this the log cannot be read in
  order, and "recent" or "old" becomes a guess.
- **programme** — which initiative it belonged to, so entries can be grouped
  without re-deriving the mapping from memory or from `docs/project/`, which
  by then no longer has the directory.
- **project** — the mc workarea name. This is the join key back to git
  history: `git log --all -- docs/project/<programme>/<project>` (or its
  pre-archive path) is how a real question about *why* gets answered, once
  the one-line summary here isn't enough.
- **outcome** — `delivered`, `abandoned`, or `superseded`. A closed project
  is not always a finished one, and "what happened to it" is usually the
  first question, before "what did it do".
- **summary** — one line: what it built, or why it stopped. Enough to decide
  whether to dig further, not a report.
- **doc** — the path under `docs/technical/` that now describes the resulting
  architecture, per the close-out rule in `README.md`. `none` when the project
  produced no lasting architecture (abandoned before shipping, or superseded
  before merging).
- **pointer** — a PR number, commit SHA, or short git-log-worthy reference
  into the history that this row summarizes. The row is a one-line index,
  not the record; this is how a reader gets from the index to the record.

## Log

| date | programme | project | outcome | summary | doc | pointer |
|---|---|---|---|---|---|---|
| 2026-08-29 | mc | mc-ui | delivered | Made bare `mc` the one page — NOW, QUEUE, DECISIONS, INTAKE, WORK with the menu under it — in 0.1 s offline instead of 1.9 s, gave the runner and the foreground verbs the two files that say what is running this second, and removed every other surface that printed a list. | [docs/technical/mc-ui.md](../technical/mc-ui.md) | [#430](https://github.com/martinforsberg81/memoro-cli/pull/430), [#435](https://github.com/martinforsberg81/memoro-cli/pull/435), [#440](https://github.com/martinforsberg81/memoro-cli/pull/440), [#441](https://github.com/martinforsberg81/memoro-cli/pull/441), [#443](https://github.com/martinforsberg81/memoro-cli/pull/443) |
| 2026-08-29 | mc | mc-ui-polish | delivered | Gave the page a palette: a step kind and a plan status one colour each wherever they are printed, the header, the five sections and the clock painted for meaning, every escape added after the width was decided — no new section, datum or flag, and the plain page unchanged but for one always-drawn `●` on a DECISIONS row. | [docs/technical/mc-ui.md](../technical/mc-ui.md) | [#446](https://github.com/martinforsberg81/memoro-cli/pull/446) |
| 2026-08-29 | mc | mc-helper | delivered | Gave production a reader: `mc helper --collect` writes a daily digest of five memoro sources into `~/mc/intake/errors-<date>.md` with the delta against the previous one — the digest is its own state, because memoro records no baseline anywhere — and `mc helper` then runs one headless Sonnet turn that writes proposals nobody acts on but Martin. `mc run` runs it once a day, gated by its own runs.tsv row; `mc brief --collect` lists the proposals and the page shows the loud lines. Retiring memoro's `sync-todo.mjs` (decision mc-2, on or after 2026-09-05) is handed to a memoro-side plan. | [docs/technical/mc-helper.md](../technical/mc-helper.md) | [#424](https://github.com/martinforsberg81/memoro-cli/pull/424), [#439](https://github.com/martinforsberg81/memoro-cli/pull/439), [#445](https://github.com/martinforsberg81/memoro-cli/pull/445) |
| 2026-08-29 | mc | mc-run-lanes | delivered | Made `mc run` drive one lane per repository at the same time inside the one process: the round splits the queue by the repo each plan lives in, `~/mc/runner/current.json` became one `current-<repo>.json` per lane and NOW a list, the Claude quota is one sleep both lanes wait out and STOP ends both after the step each is in, and the session became a promise because a synchronous wait held the event loop for the whole budget. No new command or flag, and one repository with ready plans is still one lane. | [docs/technical/mc-run.md](../technical/mc-run.md) | [#450](https://github.com/martinforsberg81/memoro-cli/pull/450) |
| 2026-08-29 | mc | mc-tidy | delivered | Made `status: done` the whole trigger: `mc run` archives the plan (directory `git rm`'d, `project_log.md` row written unless the close-out step wrote one, one PR per repository) in the round it reads it, and at the end of that round closes every workarea whose plan is done, whose worktree is clean and whose last run merged — worktree handed back, local branch deleted, whatever the folder kept moved to `runner/log/closed/`. `queue.md` became a strict list of names that empties itself, the page and the runner agree that a workarea is a folder holding `memoro/` or `memoro-cli/`, and the two things a machine must not decide — a project archived with no `docs/technical/` note, a workarea with no plan on main — are written to `~/mc/intake/` and raised in `mc brief`. No new command, flag or prompt key. | [docs/technical/mc-tidy.md](../technical/mc-tidy.md) | [#451](https://github.com/martinforsberg81/memoro-cli/pull/451), [#454](https://github.com/martinforsberg81/memoro-cli/pull/454), [#455](https://github.com/martinforsberg81/memoro-cli/pull/455), [#456](https://github.com/martinforsberg81/memoro-cli/pull/456), [#457](https://github.com/martinforsberg81/memoro-cli/pull/457) |
| 2026-08-29 | mc | mc-brief | delivered | Made the hour Martin sits down a verb: `mc brief --collect` gathers nine sections — merged, open, every decision file with no `**Beslut:**` line, the helper's proposals, every plan on both origin/mains, what the tidying left, the runner's day, the queue — into `~/mc/brief/<date>.md` with no model in 1.5 s online and 0.2 s offline, and `mc brief` then opens a fresh foreground Opus session on that file with the `brief` role: one decision at a time, each a proposal Martin says GO to, its only write a `**Beslut:**` line. No daemon, no watcher, no inbox, and the runner neither knows nor needs it. The answer travels plan-first — the next step session writes it into PLAN.md and sets `ready`, and the collect deletes the file once no plan waits on it. | [docs/technical/mc-brief.md](../technical/mc-brief.md) | [#413](https://github.com/martinforsberg81/memoro-cli/pull/413), [#416](https://github.com/martinforsberg81/memoro-cli/pull/416), [#422](https://github.com/martinforsberg81/memoro-cli/pull/422), [#433](https://github.com/martinforsberg81/memoro-cli/pull/433) |
| 2026-08-29 | mc | mc-dormant | delivered | Executed decision mc-1: `mc pm` and `mc pm-helper` answer one line and exit 2 with their machinery kept for the later cut, and the whole `mc watch` programme — the PM round, the sessions watchman, the notices ledger and the wake queue, 29 files and 5 864 lines — is deleted together with `~/.memoro/mc/watch/`, so a refused wake is reported rather than promised to a watcher that no longer exists. `mc worker` reads `canon/roles/worker.md` instead of a catalogue mc does not install, and its overlay escalates by writing `../decisions/<project>-<date>.md` rather than messaging a PM. `mc --help` now describes one world: plans under `docs/project/`, the runner that takes their steps, `mc brief` for decisions, `mc plan` and `mc worker` for the sessions Martin drives himself. | [docs/technical/mc-dormant.md](../technical/mc-dormant.md) | [#419](https://github.com/martinforsberg81/memoro-cli/pull/419), [#423](https://github.com/martinforsberg81/memoro-cli/pull/423), [#459](https://github.com/martinforsberg81/memoro-cli/pull/459) |
| 2026-08-29 | mc | mc-merge | delivered | Made `mc merge <repo> <pr>` the one door a pull request lands through: the gate round unchanged under a new name (`mc repo merge` prints where it went and exits 2, not aliased), and `--docs` landing a pull request whose every file is under `docs/` with no suite, no lease, no worktree and no model — the file list read from GitHub rather than a local diff, a draft, a closed one and the first path outside `docs/` each refused by name, and the round written to the round log as `mode: docs`. A plan PR now lands seconds after it is opened, by the session that opened it, so the runner sees it: six such rounds on 2026-08-28/29 (memoro-cli #419, #420, #428, #429; memoro #11025, #11039), all merged, none clicked, one of them the runner's own triage. The last word a session hears was the third place that had to say so — `mc plan`'s own first prompt, not only the role and the runner's triage prompt. | [docs/technical/mc-merge.md](../technical/mc-merge.md) | [#418](https://github.com/martinforsberg81/memoro-cli/pull/418), [#425](https://github.com/martinforsberg81/memoro-cli/pull/425), [#460](https://github.com/martinforsberg81/memoro-cli/pull/460) |
| 2026-08-29 | mc | mc-plan | delivered | Made `mc plan <name>` how new work enters the system: a fresh foreground Opus session in the workarea (created from `origin/main` if missing, never tmux, never `--resume`, launched through `work-open.js` like every other verb) carrying `canon/roles/plan.md` — read from the package, never from the user's catalogue — whose deliverable is `docs/project/<programme>/<name>/PLAN.md` in a PR titled "Plan: <name>" that the session lands itself with `mc merge <repo> <pr> --docs`, because the runner's queue is built from main. The overlay reaches both tools as one body with the Coding Profile: `--append-system-prompt` for claude, `-c instructions=` for codex — a channel that already existed, so `instructionsFor` stopped dropping the overlay for codex and the planned `AGENTS.md` fallback was never needed, and nothing is written into the worktree. The role fixes the programme rule (extend, never fork), the PLAN.md shape a headless step session reads, and decisions as `../decisions/<programme>-<n>.md` proposals with one recommendation. | [docs/technical/mc-plan.md](../technical/mc-plan.md) | [#412](https://github.com/martinforsberg81/memoro-cli/pull/412), [#414](https://github.com/martinforsberg81/memoro-cli/pull/414), [#425](https://github.com/martinforsberg81/memoro-cli/pull/425), [#426](https://github.com/martinforsberg81/memoro-cli/pull/426) |
| 2026-08-30 | mc | mc-run | delivered | Moved the runner into mc: `mc run [--rounds N] [--once] [--no-merge] [--idle-sleep S]`, rounds 0 = forever, replaced 220 lines of bash calling `claude -p`, so the tool is a per-project choice through the launch adapter (`tool: codex` runs a step as `codex exec --json --sandbox danger-full-access`, its usage read from the event stream), the prompts are `canon/roles/{step,reconcile}.md` over the Coding Profile, and runs.tsv is something `mc status` reads. A round is the queue from `~/mc/queue.md` plus every ready plan on both origin/mains, one lane per repository at a time; a step is a fresh headless session in a workarea created on demand, main merged in and never rebased, and the PR squash-merged with one wait-and-retry. The runner decides nothing with a model and nothing about plans: no triage, no decision files, `waiting-decision` is simply not ready. Measured from 2026-08-28T23:28Z: 115 rows, 84 merged, none left open, one quota sleep, no rebase failure. | [docs/technical/mc-run.md](../technical/mc-run.md) | [#421](https://github.com/martinforsberg81/memoro-cli/pull/421), [#462](https://github.com/martinforsberg81/memoro-cli/pull/462), [#465](https://github.com/martinforsberg81/memoro-cli/pull/465), [#475](https://github.com/martinforsberg81/memoro-cli/pull/475) |
| 2026-08-30 | mc | mc-status | delivered | Made `mc status <name>` the one-project answer — its PLAN.md read from the workarea rather than one-step-behind main and told when the two differ, the decisions that are actually its own (its area, its name, or the programme's numbered file), its last three rows of `runs.tsv` and the open PR on its branch — in 1.7–2.2 s live and 0.13 s `--offline`, no model, nothing written. The four blocks it was planned as became `mc` instead (decision mc-3), so `status-collect.js` and the dated list-price table in `prices.js` are what the page borrows, and the bare verb says where the page went. The old session/lease/watcher board had already gone whole with mc-ui; what was left to retire was the sentence pointing at `mc --watch`, a page removed the day it landed, and its row in `docs/mc-command-matrix.md` — the test now runs every `mc …` that sentence offers. | [docs/technical/mc-status.md](../technical/mc-status.md) | [#415](https://github.com/martinforsberg81/memoro-cli/pull/415), [#427](https://github.com/martinforsberg81/memoro-cli/pull/427), [#476](https://github.com/martinforsberg81/memoro-cli/pull/476) |
| 2026-08-31 | mc | mc-test-round | delivered | Stop this repository's own selector matching a directory as a pin | [docs/technical/mc-merge.md](../technical/mc-merge.md) | [#501](https://github.com/martinforsberg81/memoro-cli/pull/501), [#502](https://github.com/martinforsberg81/memoro-cli/pull/502), [#503](https://github.com/martinforsberg81/memoro-cli/pull/503), [#504](https://github.com/martinforsberg81/memoro-cli/pull/504), [#505](https://github.com/martinforsberg81/memoro-cli/pull/505) |
| 2026-09-02 | mc | mc-run-one-project | delivered | Made a round ask the plan before it asks git: `planRefusal` puts `kindFor` — the page's own reading of the `PLAN.json` files `queue()` has already fetched from both `origin/main`s — in front of `runStep`, so a project whose plan is blocked, done or unparseable is passed over without a worktree, a `git status`, a fetch or a merge, and the same filter is re-applied when a lane stays on a project after a merged step. Those refusals leave one line for the whole round, counted by reason in the page's shape and naming the plans that do not parse, while everything the plan cannot carry — a dirty worktree, a pull request in flight, a merge that conflicted, a branch that cannot be pushed — keeps its own named line. Measured 2026-09-02: `mc run --rounds 1` against `~/mc` walked 38 projects in 51 s and started none of them; 36 of those 38 answers were already on the page, and answering them from the plans in hand is 1.2 ms. No new file, cursor or queue, and the order stays `queue()`'s. | [docs/technical/mc-run.md](../technical/mc-run.md) | [#529](https://github.com/martinforsberg81/memoro-cli/pull/529), [#532](https://github.com/martinforsberg81/memoro-cli/pull/532), [#PR](https://github.com/martinforsberg81/memoro-cli/pull/PR) |
| 2026-09-02 | mc | runner-open-prs | delivered | Made the runner ask GitHub what is open before it acts, and land through one door. A round now reads three things instead of two — `origin/main` for the queue, the worktree for the plan, and `gh pr list --state open` once per repository — and an open pull request on a project ends that project's round with a line naming it, which also covers the timed-out session and the `plan-trespass` step that each leave a branch carrying unlanded work. A workarea whose branch is already in `origin/main` by content is moved to `<name>-<n>` *before* a session starts, where the push-guard used to refuse the push after it; 44 of the workareas under `~/mc` stood on such a branch on 2026-09-02. `mergePr` and its `gh pr merge --squash` are gone: a finished step lands through `repo-merge.js` in process, and the row reads `merged` only when `merged_into` was `main` — the old merge had squashed #11250 into the branch of #11249 and logged `success,merged` while `main` received nothing. A stack is ordered by `stackOrder` or none of it lands. And the top-level `what_the_code_taught_us` became `steps[i].comments`, plain paragraphs on the step: three of the five trespasses that day were malformed entries in that shared field, and `new-user`'s plan sat unreadable on `origin/main` for a day for the same reason — a plan the schema refuses now gets a row in `~/mc/intake/unreadable-plans.md` where `mc brief` raises it, instead of a `runner.log` line nobody reads. 259 entries moved off 38 plans. | [docs/technical/mc-run.md](../technical/mc-run.md) | [#519](https://github.com/martinforsberg81/memoro-cli/pull/519), [#522](https://github.com/martinforsberg81/memoro-cli/pull/522) |
| 2026-09-03 | mc | mc-live-page | delivered | Made `mc` left open at a terminal keep telling the truth: every 30 s the rows that changed are rewritten where they stand and nothing else on the screen is written to — not the scrollback above, not the key lines, not the prompt, not the cursor in a half-typed answer. `page-frame.js` is the pure differ (relative `CSI` moves only, four cases asserted as bytes with no terminal: unchanged writes nothing at all, a changed row is one move and one `CSI 2K`, a shrinking frame keeps its footprint because `CSI M` would move rows the page does not own, and a grown frame is reprinted with `CSI 0J` because `CSI L` drops the bottom of the screen instead of scrolling into the scrollback); `page-live.js` is the loop, with the interval a floor measured from the end of the last collect so a slow read delays the next frame rather than queueing one behind it, a failed collect holding the last good frame with one line saying so, and a raw-mode reader on its own `/dev/tty` descriptor — raw because a growth frame reprints everything below the page and canonical mode hides the typed characters in the kernel. No alternate screen buffer, no configuration, no new flag; a terminal narrower than 60 columns gets the old non-live reader, rows scrolled off the top are never rewritten, and a pipe is unchanged and now pinned as bytes. Checked in a real pty on 2026-09-03 (`scripts/mc-live-page-check.mjs`, 5½ minutes, not in `npm test`): ten refreshes, the marker above the page still there once, the page on screen once, the typed `1` and its cursor intact, `stty -g` identical before and after. The refresh costs one `collectPage` — 1.1–1.8 s warm on 2026-09-03 against the 0.1 s recorded on 2026-08-29, because `plans.json` is keyed by the `origin/main` sha and the runner moves it. | [docs/technical/mc-ui.md](../technical/mc-ui.md) | [#544](https://github.com/martinforsberg81/memoro-cli/pull/544), [#546](https://github.com/martinforsberg81/memoro-cli/pull/546), [#547](https://github.com/martinforsberg81/memoro-cli/pull/547) |
| 2026-09-03 | mc | nightly-full | delivered | Red, and since when | none | [#549](https://github.com/martinforsberg81/memoro-cli/pull/549), [#550](https://github.com/martinforsberg81/memoro-cli/pull/550), [#551](https://github.com/martinforsberg81/memoro-cli/pull/551) |
| 2026-09-03 | mc | runner-truth | delivered | A runner that merges its own code hands over to it | [docs/technical/mc-run.md](../technical/mc-run.md) | [#553](https://github.com/martinforsberg81/memoro-cli/pull/553), [#554](https://github.com/martinforsberg81/memoro-cli/pull/554), [#555](https://github.com/martinforsberg81/memoro-cli/pull/555), [#556](https://github.com/martinforsberg81/memoro-cli/pull/556) |
| 2026-09-03 | mc | mc-cut | delivered | Took the session manager out from under the page: `src/` went from 281 files and 80 345 lines to 153 and 40 945, `tests/` from 263 files to 155, `docs/plans/` from 34 files to 2, and `reach.mjs` — the static import graph seeded from the page and its verbs — went from 71 % of `src/` unreached to 0 %. The order was the whole method: the verb left a router first, and only then was what nothing else reached deleted, so every cut is one revert. The routers are `src/mc-cli.js` (the page plus twelve verbs) and `src/bin-mc.js` (`mc vault`, and nothing else); the workarea `handoff` concept and the `inbox` channel went with their readers and writers. Three things survive that no verb reaches, each named rather than left to be rediscovered: vault's carcass — 9 451 lines the two `credential-domain` files import at module top level, kept so those two can load, and already unrunnable because its own pinned path lists name files this project deleted; `memoro` / `memoro-cli`, 20 files that `package.json`'s other two `bin` entries reach and that no step earned the right to delete; and four files spawned by a path literal that no import graph can see. `reach.mjs` is kept as `scripts/reach.mjs` and `npm run reach`, out of `npm test` on purpose. Still untrue and named in the note: `README.md`, `docs/onboarding.md` and `scripts/mc-release-smoke.js` all still describe the deleted product. | [docs/technical/mc-cut.md](../technical/mc-cut.md) | [#538](https://github.com/martinforsberg81/memoro-cli/pull/538), [#539](https://github.com/martinforsberg81/memoro-cli/pull/539), [#540](https://github.com/martinforsberg81/memoro-cli/pull/540), [#543](https://github.com/martinforsberg81/memoro-cli/pull/543), [#561](https://github.com/martinforsberg81/memoro-cli/pull/561), [#564](https://github.com/martinforsberg81/memoro-cli/pull/564) |
| 2026-09-04 | mc | step-parallelism | delivered | The lane count is a setting, and `mc step` is how it is set | none | 65c8e2f |
| 2026-09-04 | mc | mc-workarea-deps | delivered | Gave every place mc runs this repository's tests one dependency tree instead of a copy per place: `~/mc/node_modules`, installed once in the work root from a copy of the repository's `package.json` — minus `scripts` and `bin`, whose `postinstall` names a path that does not exist there — and `package-lock.json`, kept in step by a byte comparison of the lockfile, and above every workarea rather than inside one, because node walks `node_modules` up every parent while a `node_modules` in the checkout is visible to git and to `scripts/affected-tests.js`, where a symlink is not matched by `.gitignore` and cost 250 selected files instead of 41. The merge gate's throwaway worktree moved out of `~/.memoro/mc` to `<work root>/gate/<repo>/candidate` so it stands in that same tree and installs nothing, and `dependencyTree` replaced the existence check with node's own parent walk: a round now stops when a declared package resolves from nowhere, where `prepare: null` and a sentence in `repo-gate-table.js` used to vouch that the suite ran without a tree — false for months while the five `src/runtime/session-host/` test files went unrun and uncounted, once per round, with the reassuring line printed. Those five were deleted by mc-cut (#561) one day after they were measured, so the project's criterion was read at the level that survived them (decision workarea-deps-1): a round with no `npm ci`, no vouch line and every declared package resolving — 73 files, 952 tests, 0 red selected on #570 and 143 files, 1538 tests, 0 red full on 2026-09-03, and `mc test memoro-cli --full` at 44198c6 on 2026-09-04 green with no prepare step. | [docs/technical/mc-work.md](../technical/mc-work.md) | [#537](https://github.com/martinforsberg81/memoro-cli/pull/537), [#541](https://github.com/martinforsberg81/memoro-cli/pull/541), [#570](https://github.com/martinforsberg81/memoro-cli/pull/570), [#578](https://github.com/martinforsberg81/memoro-cli/pull/578) |
| 2026-09-04 | mc | merge-repair | delivered | Close-out | [docs/technical/mc-run.md](../technical/mc-run.md) | [#584](https://github.com/martinforsberg81/memoro-cli/pull/584), [#585](https://github.com/martinforsberg81/memoro-cli/pull/585), [#586](https://github.com/martinforsberg81/memoro-cli/pull/586), [#587](https://github.com/martinforsberg81/memoro-cli/pull/587) |
| 2026-09-04 | mc | mc-deploy | delivered | Gave production one door: `mc deploy [--dry-run]` prints what would ship — the sha of memoro's `origin/main`, what `deploys.tsv` or `/api/version` says is live, the gap in commits, and whether the nightly ever measured that tree whole — asks once at a terminal, and on a yes runs the repository's own `npm run deploy` under memoro's repository lease, so no gate round or landing moves `main` while the build reads it. No flag skips the question, without a TTY it exits 2, and the runner never calls it: deploying is Martin's word every time. `scripts/deploy.mjs` is still the whole deploy — mc reimplements no step of it, invents no flag for it and returns its exit code — and what mc adds is the record: a row in `~/mc/runner/log/deploys.tsv` written *before* the spawn saying `running` and completed after it, so a deploy that dies half-way is a row that says where it stopped rather than a gap somebody reconstructs from `/admin/deploy/logs`, and a `no`, a held lease or no terminal is a `refused` row because it is a deploy somebody meant to make. `stopped_at`, `build` and the verified `live_commit` are parsed out of four named lines of the script, tolerantly, by teeing its output rather than inheriting it. Three readers then agree because they read the same row: the page's RUNNER block draws `production <sha> · deployed <age> ago by <holder>` and yellow when `/api/version` names another sha or a `running` row is an hour old, the brief gained a *Production* section and the role a sentence saying a `main` well ahead of production with a green nightly is a deploy to propose in one line, and `helper-collect.js`'s `deployState` takes the age from the freshest of the row and the webhook log. `/api/version` became the helper's sixth source, cached to `~/mc/runner/version.json`, because the page must fetch nothing. Measured: `mc deploy --dry-run` against `~/memoro` on 2026-09-04 read `e30fd83`, live `b3e65b6` (build 23533), 21 commits to ship, the nightly 79 commits behind. Not measured: everything from the question onwards has only ever run against a faked script — the one real deploy is Martin's to type. | [docs/technical/mc-deploy.md](../technical/mc-deploy.md) | [#592](https://github.com/martinforsberg81/memoro-cli/pull/592), [#594](https://github.com/martinforsberg81/memoro-cli/pull/594), [#595](https://github.com/martinforsberg81/memoro-cli/pull/595), [#596](https://github.com/martinforsberg81/memoro-cli/pull/596) |
| 2026-09-04 | mc | live-page-rows | delivered | Ask the terminal where the page is, and draw every frame against that row | [docs/technical/mc-ui.md](../technical/mc-ui.md) | [#593](https://github.com/martinforsberg81/memoro-cli/pull/593) |
| 2026-09-04 | mc | test-nightly | delivered | mc test nightly, and mc repo nightly points at it | [docs/technical/mc-merge.md](../technical/mc-merge.md) | [#599](https://github.com/martinforsberg81/memoro-cli/pull/599) |
| 2026-09-04 | mc | no-reconcile | delivered | Removed `reconcile` as a session kind and put a merge conflict where it is cheapest. Three layers: a `.gitattributes` at the root whose one rule is `docs/project/project_log.md merge=union`, so two close-outs keep both rows with no session and no per-clone `git config` (the counterfactual measured: with the driver removed, the same two branches conflict); `plan-merge.js`, which resolves a `PLAN.json` conflict from the three sides git holds in the index during the merge (`:1:` base, `:2:` ours, `:3:` origin/main) by the rule the plan already has about who may write what — each step to whichever side changed it, a criterion met if either side met it, `goal`/`contract`/`out_of_scope` and the criteria themselves main's — and refuses rather than guesses when both sides touched the same step, a side is not JSON, or the result would fail `validatePlan`; and the residue handed to the **step** session, which had to read that code anyway. The round now reads the plan from `HEAD` while a merge is in progress instead of treating a conflict as unreadable, `chooseKind` no longer takes conflicts at all, and `stepPrompt` gains a preamble naming the files and saying the merge is the first thing it does and not the job — one session, one pull request. A plan-conflict resolution is judged against the plan on `origin/main`, not the HEAD copy the session was handed, or main's own edits would read as trespass; the resolution rules moved to `canon/roles/_common.md` and `step.md`; and where there is no step to hand a conflict to, the merge is aborted and the project skipped with the files named, as before. Of the 67 conflict events measured in `runner.log`, 37 held nothing but an append-only log, a generated artifact or a plan file and now cost no session at all (the generated-artifact share once memoro's half lands); the other 30 cost no session of their own either. That is what the era spent 64 sessions and 519 minutes of model time on. The memoro half — memoro's own `.gitattributes` and getting `npm run sdk:check` into its `affected` gate before the SDK artifacts get the driver — is `docs/project/test-architecture/generated-artifact-merge/PLAN.json` there. | [docs/technical/mc-run.md](../technical/mc-run.md) | [#607](https://github.com/martinforsberg81/memoro-cli/pull/607), [#610](https://github.com/martinforsberg81/memoro-cli/pull/610), [#613](https://github.com/martinforsberg81/memoro-cli/pull/613), [#615](https://github.com/martinforsberg81/memoro-cli/pull/615) |
| 2026-09-05 | mc | intake-inbox | delivered | Made `~/mc/intake/` an inbox that drains. A file put there — a log, a screenshot, a note — gets exactly one headless turn, which is given the file **by name** and opens it itself (`helperPrompt` no longer inlines a digest between `----- DIGEST -----` markers: a screenshot has no text to inline, and the inbox is defined by anything being droppable in it), decides one proposal or none, and the file is archived under `~/mc/runner/log/intake/<date>/` the moment its turn ends. The archive is unconditional and that is the only version that terminates — a file kept because its turn failed is a file every round after this one takes again. Two gates instead of one: `helperDue` still governs the day's collect (one digest per repository per calendar day, `kind: helper`, and no model in that half any more), while the drain asks only *is there a file?* and so a round can drain without collecting; three files a round (`INTAKE_PER_ROUND`), oldest by the date *in* the name because the collector's two filename generations do not sort against each other as strings, one row per file with `kind: intake` and the file in the name column. The runner's three tables — `unplanned-workareas.md`, `unreadable-plans.md`, `undocumented-closures.md` — left the inbox for `~/mc/runner/`, beside `held.json` and `log/`: two of them are rewritten whole every round, so a turn that read one and filed it away would find it back next round, forever, and `mc brief --collect` is their one reader. `repoOfFile` keeps deriving `repo:` from the collector's own filenames and answers `null` for anything else, where the turn decides which system it is and says so in the proposal. Verified with real turns and not only on fakes: a Swedish note, a 1400×1400 PNG of a terminal and an 11 MB log each got one turn, one row naming it and an archive; 6 MB of `/dev/urandom` produced no proposal and the line *"the file is unopenable, and there is nothing to judge it against"*; a digest turn hit the ten-minute cap and its file was archived all the same, which is the contract's central rule proven on a real timeout. Found on the way and fixed: `summariseRuns` counts a note that does not start with `success` as a failure, and every helper row ever written read `memoro,success,0-proposals` — the brief and the page had counted every helper day as a failed run since the row existed. Two things left open rather than patched here, both as proposals: `previousDigest` reads only `~/mc/intake/`, so once the backlog clears the drain takes the baseline and every digest becomes a first digest with an empty delta (measured); and the page's `readDigest` matches only the legacy unprefixed digest name and has been rendering the 2026-08-30 digest ever since the repository prefix landed (measured). | [docs/technical/mc-helper.md](../technical/mc-helper.md) | [#608](https://github.com/martinforsberg81/memoro-cli/pull/608), [#612](https://github.com/martinforsberg81/memoro-cli/pull/612), [#623](https://github.com/martinforsberg81/memoro-cli/pull/623), [#624](https://github.com/martinforsberg81/memoro-cli/pull/624) |
| 2026-09-05 | mc | role-instructions | delivered | Gave every session one place its instructions come from and one file the rules it shares are written in. `instructionsFor` in `roles.js` is the single door — profile, `canon/roles/_common.md`, then the role's own overlay — and all four launch paths call it instead of writing the join out by hand; `canon/roles/plan.md` is frontmatter only, so `planLaunch` reads the same shared file into its prompt. The shared file carries what holds for any session: turn cost, reading before deciding, the shape of a question that reaches Martin, and the two rules this project was written for — a finding that is not your job becomes `~/mc/proposals/<date>-<slug>.md` rather than a paragraph in an answer (and why that is not `~/mc/intake/`, which is the inbox for material nobody has read yet), and the route to `main` is settled by reading rather than by asking. Underscore means "not a role" in `listRoles`, so the file cannot be listed as one; it stays out of `overlay`, because `run.js` reads `overlay` to tell a missing role file from an installed one. With the rules moved up, the seven roles were cut from 330 lines to 219, and `plan` moved to `fable`, verified by launch. | [docs/technical/mc-roles.md](../technical/mc-roles.md) | [#609](https://github.com/martinforsberg81/memoro-cli/pull/609), [#611](https://github.com/martinforsberg81/memoro-cli/pull/611), [#614](https://github.com/martinforsberg81/memoro-cli/pull/614), [#625](https://github.com/martinforsberg81/memoro-cli/pull/625) |
| 2026-09-05 | mc | runner-visibility | delivered | Made `ready` mean a project the runner would actually start. There are two readings of "can this be worked on now" and only one of them reached a person: `kindFor`, a fact about the plan on `origin/main`, while the STOP file, a dirty worktree, a merge stopped in one, a held pull request, work in flight and a branch a workarea does not have were facts about this machine that nothing but `runner.log` knew — 9 827 `, skip` lines, and on 2026-09-05 both of memoro-cli's unfinished plans read `ready` in every surface while `held.json` held #612 and #614 and the whole queue was stopped. `machineState` (`status-collect.js`, beside `kindFor`) is the second reading: it asks the reasons in `runStepClaimed`'s own order so the answer is the first thing the round would hit rather than merely a true one, asks the plan first so a project the plan already refuses costs no git at all, and starts, writes and fetches nothing. The two cannot drift because they share one vocabulary — `RUN_REFUSALS` in `run-plan.js`, with every refusal in the round going through one `refuse()` helper — and three tests drive a table of twelve cases through both the round and the reading, assert every word has a case, and refuse a bare `'skipped'`. Four words carry `read: false` (`worktree`, `sync`, `role-missing`, `tool-missing`): they are outcomes of work the round does and the reading may not, so `runnable: true` means nothing on this machine is in the way, not that the round is guaranteed to start. A lease is deliberately not read and a stopped merge is deliberately `dirty` rather than a word of its own. Three surfaces now say the pair — `mc status <name>` on one status row (`ready · #614 is held before merge after a repair (since 09-03 10:00Z)`, silent where the machine has nothing to add, and `--offline` reading `GitHub could not be asked …` rather than promising a `ready` it cannot stand behind), the page's QUEUE (skips counted under the machine word, a runnable name drawn as the kind the runner would actually start), and `mc brief --collect`'s *Ready, and the runner cannot start it* — a section of its own rather than rows in *Held before merge*, because a held pull request takes one of that section's three answers and a workarea a killed session left takes a person opening it. A project the runner has a live session on is dropped from it, which running the thing found: at 2026-09-05T16:35Z it named `sql-w3-email-closure` eight minutes into its step. Measured over 2026-08-25 → 09-05: 17 of 451 step runs left the workarea dirty (3.8 %), 6 of the 12 killed at budget did, the median standstill before somebody acted was 11.3 h, and 1 017 rounds were skipped on a dirty worktree. The recommendation on whether the runner should commit such a worktree itself — leave it to a person, because each of the six hand-made recovery commits in those twelve days carries a verdict on the work that a killed session cannot write and the runner cannot infer, and two of the six were archived and never meant to land — is in #633 and is Martin's to rule on. | [docs/technical/mc-run.md](../technical/mc-run.md) | [#630](https://github.com/martinforsberg81/memoro-cli/pull/630), [#631](https://github.com/martinforsberg81/memoro-cli/pull/631), [#632](https://github.com/martinforsberg81/memoro-cli/pull/632), [#633](https://github.com/martinforsberg81/memoro-cli/pull/633), [#634](https://github.com/martinforsberg81/memoro-cli/pull/634) |
| 2026-09-05 | mc | mc-dev-protocol | delivered | Gave mc a test environment in dev and in prod, and gave the cross-worktree inventory the reader it was cut for lacking. The project opened as a question — `mc-cut` removed `mc dev` on 2026-09-03 and memoro's dev wrapper had been calling it 1 132 times since, exiting 2 into a log nobody tails — and step 1 (#628) measured it and recommended **remove** on numbers nobody disputes: 565 invocations in a month, ten of them by a person, 33 registered manifests with not one live pid. Martin then asked for `mc test dev` / `mc test prod` (ruling 9), which is the reader the measurement said did not exist, and its own closing sentence is what answers it: *"if that need comes back it comes back with a person behind it."* `mc dev` is back at three verbs — `list`, `register`, `unregister`, exactly what `invokeMcDev` speaks — and `list` sweeps what it reads, which is the half the old inventory never had. `mc test dev|prod` ensures a server (shared from the installation on `main`, `--here` for the calling worktree) and runs the suites the repository declares in `.mc/test.json`, so mc holds no list of memoro's script names. Reuse is decided by the inventory and never by a port answering: a URL that responds says something is serving, never that it is serving the tree you are about to judge. Nothing runs by itself — no round, no page, no schedule. memoro's half: one base URL for all twenty browser suites, which each had their own variable and their own copy of `http://127.0.0.1:8787` (#11452); the declaration and its two-way guard (#11456); and the write smoke, which signs in to the managed test account, writes the account's own calendar export token, reads it back, deletes it and proves the deletion (#11469). Three findings the work produced rather than assumed. **`reach.mjs` cannot see across a repository** and that is what cost the verb — written into `mc-cut.md`, with the second gap found while fixing it: the hand-kept seed list had already drifted and was calling `mc deploy`, routed and working, 379 dead lines; the table is read where it lives now and unreached went 3 files to 0. **Both wrong readings of a skip** happened within an hour: the write smoke exited 0 against production having skipped every step and was reported green, then mc deciding from `needs_account` reported a local run that did sign in and did write as never signed in — resolved by `skip_exit_code`, declared by the repository, because the suite is the only thing that knows. **Both wrong readings of a dead server** likewise: eight suites reported red on `ERR_CONNECTION_REFUSED` after memoro's worker exited mid-round, then a single health probe that did not come back under load condemned nine suites with the server up the whole time — resolved by asking before each suite and after each red one, three attempts with a timeout. Left open as a proposal: memoro's dev server exits under a long browser round, twice in three tries, `worker exited unexpectedly` code 1. And `msr-core` is genuinely red against a local server on main-ish code, which is the argument for the verb. | [docs/dev-server-protocol.md](../dev-server-protocol.md) | [#635](https://github.com/martinforsberg81/memoro-cli/pull/635), [memoro#11452](https://github.com/martinforsberg81/memoro/pull/11452), [memoro#11456](https://github.com/martinforsberg81/memoro/pull/11456), [memoro#11469](https://github.com/martinforsberg81/memoro/pull/11469) |
| 2026-09-05 | mc | brief-blocked-steps | delivered | Made blocked steps the brief's work. `mc brief --collect` gained a *Blocked* section beside the two that already mean "a project is standing still and it is yours", built from the plans *Plan status* had already parsed off both `origin/main`s — so it costs no extra git call — and grouped by what the reader does rather than by `blocked_by.kind`: named decisions get a table with the blocker's name uncut, because that name is what a session looks the answer up by; `plan-review` gets one line per programme ending in `mc plan <programme>`, because it is the park every converted plan carries and never a question for Martin; project blockers get a count, because the order they close in is the blocking project's design and this section reads it rather than moves it. Two facts nothing else saw ride under that count — a project blocker whose project has left `origin/main` (`staleBlockers`, reused rather than recomputed so the page and the brief cannot disagree) and a blocker name that is not a name (`NAME_RE` in `plan-schema.js`, defined and read by nothing until now, exported for this one reader): `sql-goal1-certification` step 4 waits on a 99-character sentence, reported and not refused, because a schema that rejected one would make every plan carrying one unrunnable the moment it landed. Measured on 2026-09-05 against the real estate, not copied from the plan: **45** blocked steps, not 48 — 28 project, 12 `plan-review` across six programmes (not eight), 5 named decisions, 0 stale, 1 unnamed. `canon/roles/brief.md` now carries the three routes and the one new authority — a brief session may set a step it settled by reading back to `ready`, with what it read written into that step's `comments` in the same edit, which makes the brief a fourth writer beside the step session, the planning session and the runner — and the route that authority travels: one pull request per repository per brief, from a worktree at `~/mc/brief/unblock/<repo>` on branch `brief/unblock-<date>`, landed with `mc merge <repo> <pr> --docs`. That was chosen over an `mc unblock` verb because it needs no new code and no new authority, and a route that has to be built first cannot be walked by the step that walks it. Both names are load-bearing and both were checked by running them: with the worktree in place `areasWithCheckout` listed 80 areas and neither `brief` nor `unblock` was among them, and `projectForBranch('brief/unblock-2026-09-05')` returned `null` against the 46 project names on main where the control `brief-blocked-steps-4` returned its project. The sentence telling a planning session that a `plan-review` park is its own went into `planLaunch`'s prompt rather than into `canon/roles/plan.md`, which is frontmatter with no body by decision and asserted so. **Not done, and the reason is the estate rather than the mechanism: the route has never been walked end to end.** On 2026-09-05 nothing was settleable by reading — all five named decisions are genuine questions of Martin's (the email-side gate on `home-on-msr`, the two i18n rulings, the lanes pair, the sql promotion), all 28 project blockers name a project with unfinished work, and the one unnamed blocker is a live ruling with a bad name rather than a dead block. Criterion 4 stays unmet and needs a blocker the estate answers. | [docs/technical/mc-brief.md](../technical/mc-brief.md) | [#644](https://github.com/martinforsberg81/memoro-cli/pull/644), [#647](https://github.com/martinforsberg81/memoro-cli/pull/647), [#649](https://github.com/martinforsberg81/memoro-cli/pull/649) |
| 2026-09-06 | mc | the-page-remade | delivered | The rest of the page: the lanes, production, WORK, INTAKE, and the order of the sections | [docs/technical/mc-ui.md](../technical/mc-ui.md) | [#657](https://github.com/martinforsberg81/memoro-cli/pull/657), [#660](https://github.com/martinforsberg81/memoro-cli/pull/660), [#663](https://github.com/martinforsberg81/memoro-cli/pull/663), [#664](https://github.com/martinforsberg81/memoro-cli/pull/664), [#665](https://github.com/martinforsberg81/memoro-cli/pull/665) |
| 2026-09-06 | mc | the-role-a-session-runs-on | delivered | A session records the role text it was given, and a verb says whether it is canon's | [docs/technical/mc-roles.md](../technical/mc-roles.md) | [#659](https://github.com/martinforsberg81/memoro-cli/pull/659), [#661](https://github.com/martinforsberg81/memoro-cli/pull/661) |
| 2026-09-06 | mc | plan-read-from-main | delivered | Made the round hand out the step `main` names. The documented contract is that a planning session and the runner share a `PLAN.json` on `main` and nothing else, and one condition quietly broke it: `planOf`'s `fromHead` was called with `conflicts.length > 0`, so *any* conflicting file made the round read the plan out of the branch — while the docstring above it argued for exactly one case, a `PLAN.json` whose own merge rule refused it and which therefore parses as nothing. Everything else it covered was a file git had already merged, main's plan sitting on disk beside the stale one being read. Measured over the whole of `runner.log` to 2026-09-05: **207 rounds reached a conflict, 180 of them (87 %) with no `PLAN.json` among the conflicting files**, 31 distinct projects, and `docx-editor` reported *"step 17 is blocked on decision docx-ime-input-source"* for 13 consecutive rounds about a step main had re-planned the evening before, its only conflict a technical note. Step 1 put the three options to a ruling rather than reversing a deliberate choice quietly, and [ruling 10](mc/rulings.md) took the narrow one: `conflicts.some(isPlanPath)`. That is the whole fix — no `git show origin/main:` read was needed, because a merge that stopped elsewhere has already written main's plan into the worktree, demonstrated in a scratch repository before the predicate was written. One existing test changed shape, and the reason is a fact about the round: since `planRefusal` stops a project whose plan on main is blocked *before* a worktree is touched, a blocked plan and a non-plan conflict can no longer meet in the same round at all. The guard beside it is what a person actually sees. A round that aborts a merge nothing could be handed now records the workarea in `~/mc/runner/unmergeable.json` — the files, a `since`, the shape `held.json` already has — because `git merge --abort` leaves the worktree **clean**, so a second later `git status --porcelain` says nothing and every surface calls the project runnable while the round conflicts and aborts again ten minutes on. No predicate over this machine could have answered it and the reading may not merge to find out; `machineState` reads the record instead, which makes `unmergeable` the one word in `RUN_REFUSALS` answered from a file the round wrote. `mc status <name>` and the brief's *Ready, and the runner cannot start it* then say it with no surface of their own, and the abort line no longer quotes the stale plan. Verified live on a scratch estate with a real bare origin and a real conflicting workarea: the round logged the abort, answered `skipped:unmergeable`, wrote the entry, left the worktree clean, and both surfaces read `blocked · origin/main could not be merged into … (since 09-06 14:22Z)` until the merge was resolved by hand, when the entry dropped and both went quiet. Left open, deliberately: the case `fromHead` legitimately fires on is *also* stale — `HEAD` there is the copy that lags, and `sdk-artifact-storage` (13 of the 27 plan-conflict rounds) reads `blocked` on a step main says is `done` — which is `branch-landed.js`'s ground and has its own proposal; and the round aborts merges for four other reasons (role missing, tool missing, STOP, an UPDATE while waiting for a lane) without recording anything, because there the merge is not what a person has to act on. | [docs/technical/mc-run.md](../technical/mc-run.md) | [#629](https://github.com/martinforsberg81/memoro-cli/pull/629), [#668](https://github.com/martinforsberg81/memoro-cli/pull/668), [#669](https://github.com/martinforsberg81/memoro-cli/pull/669), [#670](https://github.com/martinforsberg81/memoro-cli/pull/670) |
| 2026-09-06 | mc | total-lane-cap | delivered | Gave the machine a lane number of its own. `mc run lanes --total <n>` sits beside `per_repo` in `~/.memoro/mc/lanes.json`, and the two are not the same kind of thing: `per_repo` bounds how many landings meet at one `main` and is a correctness number, `total` bounds this machine — CPU, memory, quota, and how much of a fleet one person can follow — where **a count of sessions is a proxy for load, not a measurement of it**. `lanes 3` on two repositories was six Opus sessions and no line of code objected. Absent still reads as no cap, so an operator who sets nothing gets exactly what they got before. The two bind differently and that is the whole of how they interact: `per_repo` is structural — that many lane loops per repository — while `total` is an in-process claim each lane takes at the last moment before it launches a session and drops in the same `finally` that removes its `current-<repo>.json`, so what the page shows and what the cap counts cannot drift. It is a claim and not a count of files because a file is written after a step begins, and two lanes counting in one tick would both see the same free slot; a test starts more lanes than the total permits against a blocking step and asserts how many actually began. A lane with no slot polls every 15 s, says one line rather than one per attempt, and gives the wait up on STOP or UPDATE — the drain promises that from the moment UPDATE is read no lane starts a step. The bare verb was the deliverable as much as the cap: `lanes 2 per repository, 3 in total — 3 in flight`, because the old line named only the per-repository number and that is what produced the wrong picture. [Ruling 14](mc/rulings.md) set the pair to `per_repo 2, total 3` on 2026-09-05, lifting the interim `per_repo 1`: a repository can never hold more than `per_repo` slots, so `2/3` guarantees memoro-cli one lane memoro cannot take, where `3/3` would guarantee it nothing against 46 memoro step runs to memoro-cli's 10 since 2026-09-02. Measured from `runner.log`'s own start lines over the first 12½ hours under the pair: 40 step sessions, never more than three at once, three at once six times; the same reading of the day before the cap existed has six. Left open, deliberately: no ordering rule between waiting lanes — no lane has yet had to wait, so the ceiling is measured and the waiting is still only tested — and the total counts steps only, not the chores beside the lanes and not a second `mc run` process, which keeps a counter of its own. Found on the way and not fixed here: since #665 the page's RUNNER block draws one row per repository, so a second lane in the same repository is drawn nowhere — three steps in flight, two rows (`~/mc/proposals/2026-09-06-runner-block-drops-lanes.md`). | [docs/technical/mc-run.md](../technical/mc-run.md) | [#636](https://github.com/martinforsberg81/memoro-cli/pull/636), [#637](https://github.com/martinforsberg81/memoro-cli/pull/637), [#638](https://github.com/martinforsberg81/memoro-cli/pull/638), [#639](https://github.com/martinforsberg81/memoro-cli/pull/639), [#PRNUM](https://github.com/martinforsberg81/memoro-cli/pull/PRNUM) |
